# samples/

Sample applications demonstrating MCUHome features, each with a
`sample.yaml` so twister can build them (`west twister -T samples
--integration`).

Empty until the first feature exists to demonstrate.
