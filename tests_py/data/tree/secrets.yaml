# Test fixture: the secrets store of the fixture configuration tree.
# Nothing sensitive lives here — real secrets never enter the repository
# (see THREAT_MODEL.md). The commissioning credentials below are a
# throwaway set in the shape `mcuhome init-pairing --secrets` produces,
# and no device that ever leaves a desk uses them.
device_label: Bench Node

bench_node_discriminator: 2314
bench_node_passcode: 84920174
bench_node_salt: "GBG/P9QwOwhgSLc1fnDR7FWutk+sSsOutub53NjXsp8="
