# SPDX-FileCopyrightText: 2026 The MCUHome Contributors
# SPDX-License-Identifier: Apache-2.0
#
# Generated by mcuhome from 00-bmp180-two-endpoints.yaml — do not edit.
#
# Kconfig fragment for device "bmp180-node". Every symbol below follows from the configuration;
# board-level and snippet-level settings are not repeated here.
#
# This application needs the snippets it was generated for: matter, boot-mode. CMakeLists.txt next
# to this file spells out the build command that applies them.

CONFIG_BT=n
CONFIG_CHIP=y
CONFIG_CHIP_DEVICE_SOFTWARE_VERSION=65536
CONFIG_CHIP_DEVICE_SOFTWARE_VERSION_STRING="0.1.0"
CONFIG_CHIP_ENABLE_PAIRING_AUTOSTART=y
CONFIG_CHIP_OTA_REQUESTOR=y
CONFIG_ENTROPY_GENERATOR=y
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_FLASH_PAGE_LAYOUT=y
CONFIG_IMG_BLOCK_BUF_SIZE=1024
CONFIG_IMG_MANAGER=y
CONFIG_MBEDTLS=y
CONFIG_MBEDTLS_PSA_CRYPTO_C=y
CONFIG_MCUBOOT_IMGTOOL_SIGN_VERSION="0.1.0"
CONFIG_MCUHOME=y
CONFIG_MCUHOME_MATTER=y
CONFIG_MCUHOME_MATTER_MAX_DYNAMIC_ENDPOINTS=2
CONFIG_MCUHOME_MATTER_OTA=y
CONFIG_MCUHOME_MATTER_THREAD_ROUTER=y
CONFIG_MCUHOME_SENSOR=y
CONFIG_NET_L2_OPENTHREAD=y
CONFIG_REBOOT=y
CONFIG_SENSOR=y
CONFIG_SPI=y
CONFIG_SPI_NOR=y
CONFIG_SPI_NOR_FLASH_LAYOUT_PAGE_SIZE=4096
CONFIG_STD_CPP17=y
CONFIG_STREAM_FLASH=y
CONFIG_STREAM_FLASH_ERASE=y

# This device's commissioning identity, emitted as one indivisible group
# by mcuhome.pairing.kconfig_lines(). CHIP takes the passcode and the
# SPAKE2+ verifier derived from it as two unrelated symbols and checks
# neither against the other on Zephyr, so a build that changed one and
# not the other would flash, boot, advertise itself and then refuse
# every commissioner. Nothing here may be edited by hand: change the
# credentials in the device configuration and rebuild.
CONFIG_CHIP_DEVICE_VENDOR_ID=65521
CONFIG_CHIP_DEVICE_PRODUCT_ID=32768
CONFIG_CHIP_DEVICE_DISCRIMINATOR=0xF00
CONFIG_CHIP_DEVICE_SPAKE2_PASSCODE=20202021
CONFIG_CHIP_DEVICE_SPAKE2_IT=1000
CONFIG_CHIP_DEVICE_SPAKE2_SALT="U1BBS0UyUCBLZXkgU2FsdA=="
CONFIG_CHIP_DEVICE_SPAKE2_TEST_VERIFIER="uWFwqugDNGiEck/po7KHwwMwwqZgN10XuyBajPGuyzUEV/iree4lOrao5GuwnlQ65CJzbeUB49s31EH+NEkg0JVI5MGCQGMMT/SRPFNRODm3wH/MBiehuFc6FJ/NH6Rmzw=="

# Framework-owned CHIP settings, reached through the wrapper stage 4
# writes next to this file (include/CHIPProjectConfig.h).
CONFIG_CHIP_PROJECT_CONFIG="include/CHIPProjectConfig.h"
