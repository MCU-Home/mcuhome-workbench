# SPDX-FileCopyrightText: 2026 The MCUHome Contributors
# SPDX-License-Identifier: Apache-2.0
#
# Generated by mcuhome from 00-bmp180-two-endpoints.yaml — do not edit.
#
# Zephyr application for device "bmp180-node". The MCUHome runtime is consumed as a Zephyr module,
# so this file only names the generated device configuration and the framework's generic application
# main — all behavior lives in the module.
#
# Generated for board nrf7002dk/nrf5340/cpuapp, snippets: matter. Building it for anything else is
# not a supported configuration: the tables reference this board's devicetree nodes by name.

cmake_minimum_required(VERSION 3.20.0)

find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(bmp180-node LANGUAGES C)

target_sources(app PRIVATE
    ${ZEPHYR_MCUHOME_MODULE_DIR}/app/src/main.c
    ${CMAKE_CURRENT_SOURCE_DIR}/src/mcuhome_config.c
)

target_include_directories(app PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
