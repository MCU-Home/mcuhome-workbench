# boards/

Out-of-tree Zephyr board definitions for hardware that upstream Zephyr does
not cover. Registered with the build system through `board_root` in
[zephyr/module.yml](../zephyr/module.yml).

Empty until the first custom board port is needed; boards supported by
upstream Zephyr require no entry here.
