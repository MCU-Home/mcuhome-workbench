# drivers/

Out-of-tree Zephyr device drivers (each with its own Kconfig, CMakeLists.txt
and devicetree binding under [dts/bindings/](../dts/bindings/)).

Empty until the first driver is implemented.
